<Grid x:Class="GameRes.Formats.GUI.CreateDpngWidget"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:p="clr-namespace:GameRes.Formats.Properties">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*" MinWidth="100"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Label Grid.Column="0" Grid.Row="0" 
           Content="Canvas Width:" 
           HorizontalAlignment="Right" 
           VerticalAlignment="Center" 
           Margin="5"/>
    <TextBox Grid.Column="1" Grid.Row="0" 
             Name="CanvasWidthBox" 
             Text="{Binding Source={x:Static p:Settings.Default}, Path=DPNGCanvasWidth, Mode=TwoWay}" 
             HorizontalAlignment="Left" 
             VerticalAlignment="Center" 
             Width="100" 
             Margin="5"/>

    <Label Grid.Column="0" Grid.Row="1" 
           Content="Canvas Height:" 
           HorizontalAlignment="Right" 
           VerticalAlignment="Center" 
           Margin="5"/>
    <TextBox Grid.Column="1" Grid.Row="1" 
             Name="CanvasHeightBox" 
             Text="{Binding Source={x:Static p:Settings.Default}, Path=DPNGCanvasHeight, Mode=TwoWay}" 
             HorizontalAlignment="Left" 
             VerticalAlignment="Center" 
             Width="100" 
             Margin="5"/>

    <CheckBox Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="2"
              Name="AutoDetectCheckBox"
              Content="Auto-detect canvas size from tile positions"
              HorizontalAlignment="Left"
              Margin="10,5"
              IsChecked="{Binding Source={x:Static p:Settings.Default}, Path=DPNGAutoDetectCanvas, Mode=TwoWay}"/>
</Grid>