<Window x:Class="SchemeEditor.ByteArrayEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Byte Array" Height="400" Width="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="InstructionText" 
                   Text="Enter hex values (e.g., FF 00 AA BB):" 
                   Margin="0,0,0,5"/>
        
        <ComboBox Grid.Row="1" x:Name="FormatCombo" SelectedIndex="0" 
                  Margin="0,0,0,5" SelectionChanged="FormatCombo_SelectionChanged">
            <ComboBoxItem>Hex</ComboBoxItem>
            <ComboBoxItem>Text (UTF-8)</ComboBoxItem>
            <ComboBoxItem>Base64</ComboBoxItem>
        </ComboBox>
        
        <TextBlock Grid.Row="2" x:Name="FormatHelpText" 
                   Text="Spaces, commas, and dashes are ignored. Odd-length values are padded with leading zero."
                   FontSize="10" Foreground="Gray" TextWrapping="Wrap"
                   Margin="0,0,0,10"/>

        <TextBox Grid.Row="3" x:Name="DataTextBox" 
                 TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" 
                    Margin="0,10,0,10">
            <Button Content="Load from File" Width="100" 
                    Click="LoadFromFile_Click" Margin="0,0,5,0"/>
            <Button Content="Save to File" Width="100" 
                    Click="SaveToFile_Click" Margin="0,0,5,0"/>
            <TextBlock x:Name="StatusText" 
                       VerticalAlignment="Center" 
                       Margin="10,0,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Margin="0,0,10,0" 
                    Click="OK_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="75" 
                    Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>