<!-- ArrayEditDialog.xaml -->
<Window x:Class="SchemeEditor.ArrayEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Array" Height="400" Width="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TypeInfoText" 
                   FontWeight="Bold" Margin="0,0,0,5"/>
        
        <TextBlock Grid.Row="1" x:Name="InstructionText" 
                   Text="Enter values:" 
                   Margin="0,0,0,5"/>

        <TextBox Grid.Row="2" x:Name="DataTextBox" 
                 TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    Margin="0,10,0,10">
            <Button Content="Load from File" Width="100" 
                    Click="LoadFromFile_Click" Margin="0,0,5,0"/>
            <Button Content="Save to File" Width="100" 
                    Click="SaveToFile_Click" Margin="0,0,5,0"/>
            <TextBlock x:Name="StatusText" 
                       VerticalAlignment="Center" 
                       Margin="10,0,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Margin="0,0,10,0" 
                    Click="OK_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="75" 
                    Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>