<Window x:Class="SchemeEditor.AddSchemeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add New Scheme" Height="200" Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Scheme Name:"/>
        <TextBox Grid.Row="1" x:Name="SchemeNameTextBox" Margin="0,0,0,10"/>

        <Label Grid.Row="2" Content="Scheme Type:"/>
        <ComboBox Grid.Row="3" x:Name="SchemeTypeComboBox" 
                  VerticalAlignment="Top" Margin="0,0,0,10"
                  DisplayMemberPath="Name"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Margin="0,0,10,0" 
                    Click="OK_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="75" 
                    Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>